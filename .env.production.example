# Production on home server (192.168.0.146) — copy to .env on the server
NODE_ENV=production
PORT=3010

# Postgres runs on the same host as the app
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"
